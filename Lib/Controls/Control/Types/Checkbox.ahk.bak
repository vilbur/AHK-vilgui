/** Class Checkbox_vgui
*/
Class Checkbox_vgui extends Control_vgui{

	;;is_checked := false
	;/** new instance_vgui of Checkbox
	;*/
	;new($name:=""){
	;	return % this.Clone()._type("Checkbox").name($name)
	;	;return % new this().setParentGui(this.ParentGui)
	;	;					._type("Checkbox")
	;	;					.name($name)
	;	;					;.value($name)
	;}
	;/** Add to GUI
	;	Move value to checked
	;	Move label to value of control
	;*/
	;add($name:=""){
	;	this.name($name)
	;	this._setToEmptyValue($name)
	;	return % this.addControl()
	;}
	/** checked
		@param int|string $key of checked item
	*/
	checked($checked:=true){
		;this.is_checked := $checked
		this._Options.options .= " checked" ($checked == true)
		return this
	}
	;/** _isChecked
	;	@param int|string $key of _isChecked item
	;*/
	;_isChecked(){
	;	return % " checked" (this.is_checked == true)
	;	;return % " checked1"
	;
	;}


}